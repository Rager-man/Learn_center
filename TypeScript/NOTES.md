# TypeScript 课程 · 导师备忘

> 给 AI 导师的工作记忆：记录学员进度、踩过的坑、已纠正的观念。每次课后更新，新课开工前先读这份。

## 学员档案

- 背景：Python 基础，JS/TS 零基础。中文交流，术语保留英文。Obsidian 用户。
- 环境：macOS，Node 22，工作区 `/Users/clock1/Project/Learn_center/TypeScript`（npm 项目在根目录，TypeScript 7.0.2，tsconfig 三开关：strict / noEmit / types:["node"]）。

## 进度总览

- **第 1 课 ✓ 结业（2026-08-31）**：三任务全过（任务 3 收尾由导师代为归位），复盘三题通过，`tsc --noEmit` 沉默。
- **第 2 课 ✓ 结业（2026-09-01）**：两任务全过（id 复用 bug 与"显式 undefined"两处均自修；3 处装饰清理导师代劳），复盘三题通过（题 2 伏笔补讲后确认看懂，题 3 拆解后答对）。
- **第 3 课 ✓ 结业（2026-09-03）**：两任务全过（必改 A/B/C 两轮修净；两处静默坑真修——`firstScore(小美)` 实测打出 0、ex6 轮数确定性验证乱敲不计轮），复盘三题通过（题 1、2 直接过；题 3 与 typeof 补版**补讲后一次确认**）。
- **第 4 课 ✓ 结业（2026-09-03）**：两任务全过（判卷眼坏 2 首轮失守——ex8 用 `{...data, amount} as Order` 缝口、第 6 层逐支检查整层缺失；整改清单 5 条一轮修净）。复盘三题通过（题 3 直接过；题 1、2 补讲后一次确认）。
- **第 5 课 ✓ 结业（2026-09-05）**：三任务全过（ex11 必败版演示首轮缺失——整改 3 条一轮修净；复盘三题通过：题 2 首答直过，题 1 后半问与题 3 机制补讲 + 选择题一次确认）。详见下方第 5 课记录。
- **第 6 课 ✓ 结业（2026-09-06）**：两任务全过（代码层整改一轮修净；文字清单三轮到照抄级修净，末位一词组导师代改；复盘三题通过：题 2 首答直过，题 1 处理半问与题 3 "之后"缺因补讲 + 选择题一次全对）。详见下方第 6 课记录。
- **第 7 课 ✓ 结业（2026-09-09）**：两任务全过（纸面展开漏 `?` 为核心病灶，补讲 + 整改收尾；死 import 与隐式 any[] 导师代劳；两处报错留痕经导师复跑复核**一字不差**——判卷眼"抽查改动语义"首次全面通过）。复盘三题通过（题 3 首答直过；题 2 补讲后选择题一次过；题 1 选择题首答失手——**课程首次**，选 A；替换链二次补讲 + 确认题 (a)(b) 全对）。详见下方第 7 课记录。
- **第 8 课 ✓ 结业（2026-09-11）**：两任务全过（核心病灶"模块化半边跳过"——export 加在 ex7/ex14 档案上、测试 import 直插档案、npm test 副作用 stdout 现行为证——两轮整改收口；死 import 第三次被"先扫 import 区"首战命中；snake_case 第四次回潮一轮修净；留痕一行导师代改、装饰六处代劳）。复盘三题通过（题 1/3 首答直过，题 3 自带"单独测试"反哺；题 2 选择题 C 一次过 + test 5 提名成立）。全角标点零事故——前科背景下首次干净卷面。详见下方第 8 课记录。
- **第 9 课 · 开工（2026-09-11）**：综合实战——待办事项完整版（判别联合 + 文件持久化 + zod 读回校验 + 多文件 + 测试首次全上）。骨架 ex18_todo/ 均绿出生（tsc 沉默 / npm test 4 文件 18 测试 = 15+2 数据结构+1 占位 / tsx 无输出）；root package.json 加 todo 脚本（本地不入库）。判卷要点见第 8 课记录"教学观察（第 9 课要用）"。待学员练习与判卷。

## 第 1 课记录

**踩坑与事件（均已修，课文已同步）**：

- TS2591（Cannot find name 'process'）：TS 7 不自动装载 `@types/*`，tsconfig 必须显式 `"types": ["node"]`。
- tsc 带文件名被 TS5112 拒绝；单文件编译用 `--ignoreConfig`。规则一句话：**tsx 认文件，tsc 认项目**。
- ex1 错误行留着导致项目门禁不绿——课文任务 1 已补"观察完注释掉错误行"。

**已纠正的观念**（后续课次留意回潮）：

- "尽量给所有变量写注解" → 已掰正为**边界写死、内部放手**（关键论据"不写注解 ≠ 不检查"）。曾把 grade 返回值放宽成 `string`，已自己改对。**第 2 课讲 interface/type 时复查这个习惯。**
- 曾以为 JSON.parse 实验那两行会编译报错 → 已修正。认知存证由行内三处答案合成，全部准确：any→编译器沉默；nama≠name→undefined；undefined 无 length→TypeError。"注解是声称，数据是事实"已到位。

**教学观察**：

- 交作业积极、数量超额（8 条报错 vs 要求 5 条）；用自己的话翻译报错（好习惯）。
- **任务会做但容易放错文件**（size 函数写进 ex2、观察注释写进 tmp 产物）——布置任务时把"改哪个文件"说到文件级。
- 对"认知存证/写观察"这类元任务，需要"是什么/怎么做"级的说明才好下手——第 2 课课文指令按此标准写。

## 第 2 课记录

**踩坑与事件（均已修）**：

- **id 复活复用 bug**：add 从"数组尾部 id+1"推导，删除尾部联系人后再 add 会拿到用过的 id（实测反例：Alice=1,Bob=2 → remove(2) → David 又拿到 2）。学生自修为 `let nextId = 1` + `id: nextId++`。建模教训已讲：id 必须单调递增，不能从当前数据推导。
- **属性简写把"缺席"变"显式 undefined"**：`{ phone }` 在未传时打出 `phone: undefined`（字段存在）。学生自修为条件展开 `...(phone === undefined ? {} : { phone })`，修复后字段整个缺席。
- 演示区漏 update 调用 → 学生补上；3 处装饰清理（草稿死注释 / 实验残留 / 双打印）两轮未动 → **导师代清**。此后装饰性清理一律导师直接代劳，不占来回。

**亮点**：

- update 参数 `Partial<Omit<Contact, 'id' | 'tags'>>`——超前用第 7 课武器，Omit 掉 id 是正确建模判断（展开口述未验证，第 7 课回炉 Contact 时补验）。
- `nextId++` 后缀自增、`if (contact)` 真值窄化——均无师自通（后者是第 3 课招式，判卷时已点破）。
- 实验 1 的"镜子"照出 update 走 Object.assign 直接改属性；已顺带补充：Object.assign 签名不做 readonly 检查（拦截盲区，深讲留第 7 课）。

**教学观察（第 3 课要用）**：

- **整改指令必须原子化**：一次 5 项清单学生只修了显眼 2 项；第二轮给到"行号 + 改法代码"手把手级后修完。以后整改清单带行号带代码。
- **抽象题先拆解再问**：开放反思题（"哪个返回类型最难写"）首答"没太看懂"，拆成四选一表格后立即答对。与第 1 课元任务观察一致。
- 题 3 措辞"判断返回值类型"已校正为**"判断值在不在"**（类型永远是同一个联合，判断的是此刻落在哪一半）——这句就是窄化的定义，第 3 课开场直接引用。

**伏笔存档（第 3 课开场素材）**：

- `T | undefined` 已三次出场（phone 访问、find、update 的 old）——开场可让学生自己数，数完引出"窄化"。
- "缺席 vs 显式 undefined"学生亲手修过（条件展开）——讲 undefined 语义时的第一手素材。
- `if (contact)` 已是他会的招——第 3 课从"你已经会的这一招"扩展到整套窄化工具箱。
- 课文引用的报错码（TS18048 / TS2540 / TS2339 / TS2322 / TS2493）均已实测校准。

## 第 3 课记录（2026-09-03 结业）

**踩坑与事件（均已修）**：

- 整改清单"一项含两处只修一处"：小点 2（两处冗余 `s?.`）只修了 L59 漏了 L66——与第 2 课"5 项只修 2 项"同型。**此后整改清单每个独立小点单独编号，不再合并。**
- `?.` 写进守卫内部两处（`if (s.nickname !== undefined)` 分支里还写 `s.nickname?.toUpperCase()`）——"不信任自己的窄化"。导师代清；第 4 课开头可顺口复查这个观念（证据生效范围内不需要再上保险）。
- snake_case 命名（`answer_number` / `round_count`，Python 手感）已提醒；**第 4 课起按 camelCase 判**。
- 空回车 `Number("") === 0` 的选做加固未做（不拦结业）——**第 4 课手写解析器时此坑正式回归，必做**。
- 复盘拆解首答两处偏：`!` 场景选"都不允许"（把课程纪律当语言法律）；typeof 补版选"三元不括"（运算符优先级静默坑：`+` 先拼 `"S-string"`，`=== "string"` 永远 false，三元永远走 else 且编译器不拦）。均补讲后一次确认。**新增教学点：混合运算符括号不要省——后续留意回潮。**

**亮点**：

- **top-level await 自主发现**：拆掉骨架的 `async main` 黑盒，直接模块顶层 `await`（ESM + NodeNext 下合法）——第 5 课开场回收素材。
- 病灶 7 用 `toString()` 双边通吃绕开窄化（string/number 都有 toString）——聪明的合法解；typeof 版已补讲补会。
- `firstScored([])` 主动补"一个都没有"的演示；`!= null` 首次实战就用在对抗 `scores[0]` 下标签名谎言。
- 复盘题 3 后半问（比第 1 次的变化）一次答中"快捷键→责任转移"。

**教学观察（第 4 课要用）**：

- 选择题拆解继续有效：两处补讲均一次确认通过。延续"抽象题先拆解再问"。
- **学生偏差方向恒为"过严/过谨慎"**（`!` 宁可全禁、`?.` 宁可多上），未见"图省事"方向——判卷重点继续放在边界刻画，不必再花力气建风险意识。

**伏笔存档（第 4 课开场素材）**：

- `in` / `typeof` / `instanceof` 只在 §2.2 表格露脸，判别联合转正——开场可用病灶 7 的两种写法（`toString` 绕开 vs `typeof` 窄化）引出"更聪明的联合建模"。
- 病灶 3 的"签名如实 `T | undefined`"直接衔接 unknown + 手写守卫（"边界用 unknown，核心永不 any"）。
- top-level await / `rl` 黑盒 / `void main()` → 第 5 课异步开场拆。
- 空输入解析成 0 的坑 → 第 4 课手写解析器必做。
- 判卷技术备忘：`Map.get()` **直连点**报 TS2532、**先存变量再点**报 TS18048；ex6 管道喂输入在 EOF 处第二个 question 会悬空（交互游玩不受影响，勿误诊）。

## 第 4 课记录（2026-09-03 结业）

**踩坑与事件（均已修）**：

- **ex8 首轮核心病灶**：`return {...data, "amount": amount} as Order`——三病缠身：第 6 层（按 status 逐支检查独有字段）整层缺失、`as` 压嘴、spread 把原始字段黑名单式偷渡。判卷眼坏 2（paid 缺 paidAt）失守，演示打出对象而非"已拒绝"。整改清单 5 条（原子编号+行号+代码）一轮修净。**首次出现"图省事"方向偏差**（此前学生偏差恒为"过严/过谨慎"）——样本量 +1，后续课次继续观察方向。
- 演示区 `parseOrder(x) ? console.log(parseOrder(x)) : ...` 每条数据双重调用、`??` 没用上 → 改一行式 `?? "已拒绝"`。
- ex7 命名 order_1 / order_2 / order_test（snake_case 手感回潮，第 3 课 answer_number 亲戚）——本课起 camelCase 判卷，已改 order1/order2/order3。
- 装饰代劳：ex7 重复打了两遍的 trackingNo 行（导师删，未占来回）。

**亮点**：

- ex7 演示里 trackingNo 访问**主动先窄化再点**（课件提示 2 一次做对）；try/catch 里 `error instanceof Error` 窄化后才点 message——第 3 课功夫稳定在身上。
- 删 case 实验留痕准确：贴的 TS2345 原文与导师实测一字不差。
- ex8 的 `isOrderStatus` 类型谓词一次写对；漏斗前五层全对；`Number("") === 0` 坑补考通过（第 3 课 ex6 欠账清了）；`data.amount.trim()` 直存变量比参考解还紧凑。

**教学观察（第 5 课要用）**：

- 复盘题 1 表述含混（"判别联合可以不穷尽所有状态"歧义——想说"不需要防护非法状态"）→ 选择题拆解（三选一：最大的好处是"非法组合编译期写不出来"）+ "写不出来的东西不需要防护"一句定位，一次确认。
- 复盘题 2 机制链条断（结论对、"怎么防护"没说透 + 漏答"新增状态为何强制处理"）→ 四步链条补讲（case 收窄 → default 剩 never → 漏一支塞不进 → 全部红），确认语"default 多出一个状态就不是 never 了"到位。
- 建议级守门员风格未采纳（ex8 嵌套金字塔保留，补层后到四层缩进）——不拦结业，第 5 课代码里留意是否回潮。
- as 病灶的教学论证学生已消化：好数据打印正确是"碰巧字段齐全"不是"保证"——白名单才是"类型属实"。

**伏笔存档（第 5 课开场素材）**：

- top-level await（ex6 自主发现）/ `rl` 黑盒 / `void main()` → 第 5 课异步开场全部拆开。
- 第 6 课：zod = 手写守卫的自动化版；`JSON.parse('不是JSON')` 直接 throw 的错误路径（本课课件已预告"第 6 课正式处理"）。
- 第 7 课：Order 四变体重复的公共字段（amount ×4）用工具类型收拾。
- 第 8 课：ex7/ex8 两个同名 Order 合并拆模块；`next` 非法流转被拒的测试。
- 判卷技术备忘：本课报错码（TS2339/TS2322/TS2353/TS2345/TS18046 及 never 赋值版 TS2322）均已实测写入课件；`next` 返回宽 `Order`、演示点独有字段需重新窄化——课件提示 2 已讲，勿误诊。

## 第 5 课记录（2026-09-05 结业）

**踩坑与事件（均已修）**：

- **ex11 必败版演示整体缺失**（TODO 3 后半段，完成判据明文要求）——"指令多段要求漏后半"型，第 2 课"5 项只修 2 项"同型样本 +1；与"图省事"方向无关（retry 本体一遍成型）。整改清单 3 条（原子编号 + 行号 + 代码）一轮修净，必败版带计数器验证 times=3 边界语义（"共尝试 3 次，全败"）。
- **ex10 并发版写法偏离**：数组内联调用 + `.then()` 不 await——行为等价（数组元素先求值再进 all，创建确实先于 all）但违反硬性要求"先创建三变量再 await"；`.then` 悬空单已点破（此处安全因 task 永不失败，换会失败的环境即 §2.3 崩溃坑）。改写后达标。
- 思考题首交只填预测数字、无实测代码——整改补跑 801ms 对上预测 800。
- 装饰代劳：ex10 开头 `await sleep(1000)` 残留（导师删，未占来回）。
- 通用 `task(name, ms)` 代替题目要求的三个独立函数——参数化消灭重复，工程直觉好，点破未整改。

**亮点**：

- `retry` 一遍成型：`return await fn()` 一次写对（判卷眼）、`lastError: unknown`、全败 throw 最后错误——泛型签名黑盒抄得稳。
- 双重窄化全中：`r.status` 窄化 + `instanceof Error` 窄化，还带 `String(r.reason)` 非 Error 兜底；`results.length` 防御检查扁平，嵌套金字塔无回潮。
- `String.fromCharCode(65 + index)` 生成服务名——聪明的懒办法（已点破：服务名也在 value/reason 文本里）。
- ex10 预测先行 1600/800 未改，实测 1604/800/801 三数字全中。

**教学观察（第 6 课要用）**：

- **复盘题 1 后半问（"为什么有时报错有时不报"）首答空缺、题 3 机制链条断（"catch 接得住"没说 await 的角色）**——两处均选择题拆解 + 一句定位，一次确认。与第 2–4 课模式一致：现象层答得出，机制层（谁在什么条件下干什么）需要拆解支架。
- ex9 TODO 4 归因半对（说出病因不同，没说出同族共性"类型声称 vs 运行时事实"）——复盘题 1 补讲已覆盖；第 6 课 zod"类型从数据来"时回收此点。
- 方向观察更新：必败版缺失归"指令漏段"非"图省事"；`.then` 是学前阅读残留的旧世界写法——第 6 课检查 fetch 三条错误路径完整性时，继续观察"错误路径是否又漏段"。
- 日期笔误已修正：开课行曾误写 2026-09-03，实际开课与结业同为 2026-09-05。

**伏笔存档（第 6 课开场素材）**：

- `PromiseSettledResult` 判别联合（第 4 课知识官方现身，ex11 亲手用过）；`r.reason` 的 any / `JSON.parse` 的 any → 第 6 课 zod"总账"。
- fetch 三条错误路径（断网 reject / 非 2xx 要查 `res.ok` / JSON 损坏 `json()` 抛错）= 第 6 课主菜；本课 `fetchUser` mock 换真 fetch。
- `retry` 的 `<T>` 黑盒第 7 课转正（泛型）；`.then` 点破记录：top-level await 环境下 await 是主写法。
- 判卷技术备忘：三文件 tsc 沉默由导师跑通验证；tsx 直跑不查类型（忘 await 点属性打出 undefined 是剧本，勿误诊）；计时数字 1604/800/801、all 短路 101ms、unhandledRejection 退出码 1 均实测。

## 第 6 课记录（2026-09-06 结业）

**踩坑与事件（均已修）**：

- **"多段指令部分执行"第 5、6 次样本，且蔓延到纯文字清单**：第一轮 6 条整改中 TODO 6 成功分支漏后半（正常输入只打出当天日期，今明温度全缺）、safeParse 失败分支漏句内"然后 process.exit(1)"（失败以退出码 0 谎报成功）、ex13 进口 #2 漏数（API 响应缺席）——课件 §0 的"丑话"预警未能阻止。第二轮 4 条文字整改只落实 1.5 条（L16 仅机械换数字、L17 加三字了事）。第三轮升级照抄级（4 段替换文本逐段给出）后抄了 3 段，L31 一个词组仍未动——**导师代改**（理解已由复盘选择题 Q1 独立证明，代改记录在案）。**教学结论：口头警告对此偏差无效；照抄级（替换文本级）是当前有效底线；第 7 课起判卷按骨架 TODO 逐条对账，整改清单默认给替换文本。**
- **判卷眼 2 处错标**：ex12 留痕③把 zod 第四关写成"JSON损坏"（整改已修）；ex13 两套编号混用且整改中冒出新错标（999 补前"第②关"被改成"第③关"——换数字不改理解）。**判卷眼升级：抽查"改动的语义"，不只看"是否改了"。**
- **parseCoords 边界等号 bug**：范围判断 `<` 漏等号——纬度 90、经度 180 两个合法坐标被误拒（"本课讲边界，边界等号漏了"）。改 `<=` 后 90/180 放行复验通过。
- 装饰代劳：死 import `node:path`、草稿死注释、`args_0`/`args_1`（snake_case 第三次回潮：answer_number → order_1 → args_0）、报错文案"维度"笔误。

**亮点**：

- parseCoords 三关关序正确，`Math.abs` 一行收编正负区间；四关打印彼此分得清（catch 标"路径 1 或 3"+message、路径 2 带 status、zod 报 message+path）。
- `raw: unknown`、`z.infer`（全程未手写数据 interface）、`instanceof Error` 窄化、失败路径 exit(1)——第 4/5 课功夫稳定在身。
- ex13 的 999 补前（走完一趟网络被 400 打回）/ 补后（本地拦下、网络都没碰）对比留痕到位——"错误离根因的距离"体感拿到；一句结论到位（"把关口修在外部参数进口处拦下，从源头终止错误"）。
- 复盘题 2 首答精准："不用维护两套——一套 interface 的数据类型维护，另一套对 interface 数据的类型防护"——两份真相一口气说准。
- 北极 90 116.4 实测 -3.8°C——边界放行的活数据点。

**教学观察（第 7 课要用）**：

- 复盘题 1 处理半问缺（三路径现象全对，"谁接、接完干什么"没出场）、题 3 "之后"只答顺序不答原因——延续"现象层答得出、机制层需支架"；两道补讲选择题**一次全对**（A/B），拆解支架第六次生效。
- 复盘三题第三次催收才交——**第 7 课把复盘口述提前到练习判卷之前收**。
- 术语精确度待提升（"网页响应错误"→非 2xx、"JSON 文件"→响应体）——补讲已修，第 7 课判卷留意。
- 整改执行呈"表面完成"倾向（换数字不改句、加三字算标注）——第 7 课交付前要求学员自报"清单第几条动了哪行"。

**伏笔存档（第 7 课开场素材）**：

- `retry` 的 `<T>` 黑盒转正（第 5 课埋）；开场点破：**第 6 课全程在泛型库里游泳**——`z.infer<typeof weatherSchema>`、`z.array(z.number())`、safeParse 返回的 `ZodSafeParseResult<T>` 全是泛型。
- 第 8 课拆模块现成素材：ex12 单文件拆 api/schema/cli、parseCoords 归属。
- §5 选做两项未做（Promise.race 超时、parseCoords 判别联合改造）——不拦结业，可作第 7 课学余力题。
- 判卷技术备忘：39.9 116.4 实测今 31.7/22.3、明 30.7/21.1（2026-09-06，数字随日期漂移勿误诊）；90 116.4 → -3.8°C 放行；longitude=NaN 直打 API → 400 "Given: nan"（补前佐证）；ex13 L31 补后引文"维度"为笔误修正前历史输出，勿判不一致。

## 第 7 课记录（2026-09-09 结业）

**踩坑与事件（均已修）**：

- **纸面展开漏 `?`（本课核心病灶）**：ex15 两栏展开都写成 `{ name:string; phone?:string }`——漏了 Partial 给 **name** 加的 `?`（phone 的 ? 是它原本的，不是 Partial 给的）。与复盘题 2 首答同根：把"Partial 的实现定义 `{ [K in keyof T]?: T[K] }`"当成了"Partial<Contact> 的展开"——第 2 课"展开口述"欠账只还了一半。补讲（定义 vs 展开 + 干扰项 C 恰是 Partial<Pick> 展开）后选择 2 首答全对。
- **整改照抄级第三次机械失守**：替换文本逐字给出后——①②用全角 `？` 替代半角 `?`、`__` 填空占位符未清、L37 的 `？` 加到 `string` 后面（位置错）；⑤残留重复 `③`。③④一字不差。理解已由选择 2 首答独立证明 → ①②⑤导师代改收尾（记录在案）。**已专项警告：全角标点进真代码是语法错误——中文输入法下写代码，标点必须切英文（肌肉记忆级）**。
- **复盘选择题首答失手（课程首次）**：选择 1 选 A（"`<T>` 本身含 undefined"），正确 C（`T[K]` 如实取可选属性类型）。二次补讲用**调用点替换链**（T := Contact → K := "phone" → T[K] := string | undefined）+ **用学员自己的 names 探针证伪 A**（A 成立则每次调用都该带 undefined，但 `string[]` 编译通过），确认题 (a) 替换链四步、(b) 证伪表述——全对。
- **TODO 自报规则两次未执行**（首次"太难整理"弃权、二次零行）——导师全程对账代劳。继第 6 课"口头警告无效"之后的下一级证据：**该规则对此学员无效，第 8 课起停用**，对账全导师侧，学员只需报疑问点。
- 装饰代劳：死 import `import { count } from "node:console"`（**第二次**，第 6 课死 node:path 同型，IDE 自动导入误触）；`let result = []` 隐式 any[]（演化数组特例）→ `const result: T[][] = []`。

**亮点**：

- pluck 签名一次成型（含约束与 `T[K][]`）；三签名全自推、零照抄——"从具体到泛化"的正路走通了。
- 四发探针全留痕且推断描述准确（含 phones 的 `(string | undefined)[]`——Q5 陷阱亲手踩实）。
- **两处报错留痕经导师复跑复核一字不差**（手滑 TS2345 对备课实测；漏键 TS2741 对 ex4 原始 Tag 定义方式的复现实测）——判卷眼"抽查改动语义"首次全面通过。
- groupBy 用 `key in groups` 判存在、`console.dir({ depth: null })` 主动深打——超纲好手筋。
- countByTag 双标签数全、全零仓库起点正确，输出与通讯录现状（Bob 已删）对得上；确认题 (b) 的证伪表述一次说全。

**教学观察（第 8 课要用）**：

- **"表面完成"第三型定性**：概念理解已证明后的纯机械执行失守（全角字符、占位符残留、位置错）——非理解问题，是"补痕迹"动作模式。第 8 课对策：涉及**语法字符**的整改，要求"整行重新键入回贴"而非"插字符"，试一轮看效果。
- **选择题拆解第七次使用首次失手**，替换链拆法二次即通——"占位符被替换后消失"的心象需显式维持；第 8 课讲 `ReturnType<typeof fn>` 时回收同款心象（typeof 取值类型 = 又一次"从值世界进类型世界"）。
- **复盘提前收规则有效**：首答当日到达（第 6 课催三次）——保持。
- 死 import 第二次——第 8 课拆模块 import 数量暴增，**判卷先扫 import 区**。
- TS7 与 TS5 报错差异已写入课件与开工行（TS7053/unknown、keyof 不展开、TS2739 键名无引号）——第 8 课写测试遇 vitest 类型报错时同样先实测再下结论。

**伏笔存档（第 8 课开场素材）**：

- **zod 为什么在 dependencies**（第 6 课记的那一眼）——第 8 课 npm 工程课正式算账，dependencies vs devDependencies 的现成答案。
- **ex12 单文件拆 api/schema/cli**（第 6 课埋）——第 8 课 ESM 拆模块的现成素材；parseCoords 归属也在那课定。
- 学有余力三项未做（Promise.race 超时 / parseCoords 判别联合 / **Order 公共字段 Pick 收拾**）——不追；第 8 课有 vitest 保镖后，Order 等价重构 + 测试验证是"测试是重构的胆量"的完美开场素材。
- 第 2 课"展开口述"欠账本轮已清（选择 2 + 替换链确认）；Object.assign readonly 深讲已交付（§2.5）。
- 判卷技术备忘：TS7 漏**单**键 TS2741 / 漏**多**键 TS2739（报错码随缺失数变形——复核学员留痕时按缺失数对号）；`(typeof VALID_TAGS)[number]` 定义的 Tag 在报错里打印为展开的码点排序联合（直接字面量别名则保留别名打印）；ex14 的 contacts 是独立副本（tags: `string[]`），与 ex4 的 `Tag[]` 不同——判卷勿混淆。

## 第 8 课记录（2026-09-11 结业）

**踩坑与事件（均已修）**：

- **核心病灶：模块化半边跳过**。两个模块文件（order-machine.ts / array-utils.ts）留空骨架，`export` 直接加在 ex7（4 行）/ ex14（3 行）档案上，两个测试文件的 import 绕过模块直插档案（`../../02_类型建模/ex7_order.js`、`../ex14_generics_toolkit.js`）；ex12 也被动了 3 处（export parseCoords、weatherSchema/Weather 搬顶 + export——判定为"先试从 ex12 import 后自行改正"的废弃绕路残骸）。**活体证据：npm test 每次先打出 ex7 演示区 + ex14 console.dir——"import = 执行被引文件的全部顶层代码"，模块副作用当场现形**。整改两轮收口：模块建立（与档案逐字一致）、import 改指模块、三档案 git checkout 恢复。定性：第 1 课"任务会做但放错文件"的工程版——不是不会拆，是抄了近路。整改后"安静全绿"（零演示输出）成为新验收线，学生亲手达成。
- **整改清单执行率两轮样本**："多段指令部分执行"第 7、8 次——第一轮 8 条漏 3（死 import 半行残留、ex12 漏恢复、留痕未更正），第二轮 3 条漏 1（留痕一行）。持续特征：**改显眼的主体、漏清单子项**；第 3 条给了整行替换文本仍被执行为"精准半行修"（路径换了、死名字留下）——"整行重输"政策的第二次反证。
- **死 import 第三次**（第 6 课 node:path、第 7 课 node:console 之后，首次出现在自己写的 import 里）——丑话②"判卷先扫 import 区"首战命中。
- snake_case 第四次回潮：group_arr / num_arr（answer_number → order_1 → args_0 → 本课），整改清单一轮修净。
- 留痕一行（array-utils.test.ts L95 重验补注）导师代改、装饰六处（注释残渣 di/1/f、import 双空格、assertNever 缩进、`i += n ;` 空格——最后一处是破坏实验重验的物理指纹）导师代劳，均记录在案。

**亮点**：

- **测试矩阵超纲**：15 条 it（要求 ≥8），非法流转矩阵凑齐（pending+ship / pending+cancel / paid+pay / 终态×2），终态用 `for...of allActions` 一条 it 扫全动作——学生自己提炼的 ROI 逻辑（"覆盖 ÷ 行数"）；toThrow 全部正确接箭头函数；toBe/toEqual 各就各位。
- **破坏实验留痕真实**：AssertionError 数学核验一致（i+=n+1 切 8/4 正是 [[1,2,3,4],[6,7,8]]）；实际发生但学生未点破的教学点：改坏一处只有两条红、"边界"没红（chunk(arr,0) 早退不经过坏路径）——判卷时已点破"测试只护经过路径的行为"。
- **ex17 三间屋一遍成型**：职责分居正确、迁移逐字且带出处注释（"从 ex12 第 27-47 行逐字搬"——新习惯，值得保持）、throw 坑避开（路径 2 打印原样留 api.ts）、import 区零死货；留痕经导师复跑逐字复核（北京 2026-09-11 今 30.7/16.9 明 28/17.5；无参数退出码 1）。
- 复盘题 3 首答直过且自带"单独测试"反哺（本课主题的自觉应用）；题 1 首答直过（第 6 课 zod 那一眼正式销账）。
- **全角标点零事故**——第 7 课专项警告后首次干净卷面。

**教学观察（第 9 课要用）**：

- "整行重输"政策两次被 surgical edit 绕过——第 9 课整改指令继续整行给替换文本，并**显式说"先把整行删掉"**。
- 选择题拆解支架第八次使用：一次过（第 7 课首次失手后回稳）——继续保持"抽象题先拆解再问"。
- 第 9 课 todo CLI 是最大综合实战（判别联合 + 文件持久化 + zod 读回校验 + 多文件 + 测试首次全上）——本次暴露的"放错文件/抄近路"倾向在大项目里代价更高，**开工时把"每个文件放什么"说到文件级**（第 1 课观察正式回收）。
- npm test 扫全仓库：数据结构课 `数据结构/01_线性结构/ex2_dynamic_array.test.ts` 会一起跑（Test Files 3，Tests 17 = 15+2）——判卷勿混淆；根 package.json 按 .gitignore 惯例不入库。

**伏笔存档（第 9 课开场素材）**：

- 第 9 课 todo CLI 开场回收：Task 状态 todo/doing/done 判别联合（Order 状态机的亲兄弟，ex16 刚测过的 next 模式可平移）；文件持久化 + **读回数据必须过 zod**（第 6 课"文件内容同样是边界"的正式兑现）；坏数据兜底（备份原文件 + 空库重建）呼应第 6 课 ex13"错误离根因的距离"。
- 学有余力未做不追：Order 公共字段 Pick 等价重构、vitest watch 模式、给 ex4 补测试。
- 判卷技术备忘：npm test 安静全绿 = 零演示输出（模块无副作用验收线）；数据结构课 test 入镜属正常；学生天气留痕数字 30.7/16.9、28/17.5（2026-09-11 实测，导师复跑一致）。
