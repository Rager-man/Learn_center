// 04_进阶与质量/ex15_contact_rework.ts —— 骨架（第 7 课 · 任务 2：回炉改造）
// 用法：npx tsx 04_进阶与质量/ex15_contact_rework.ts（本文件是回炉工作台，本身就是
//   纯注释的账本，直跑无输出；真正的代码改动落在 01_语法起步/ex4_contacts.ts）
//
// 剧本：第 2 课你超前写出了 update(id, changes: Partial<Omit<Contact, 'id' | 'tags'>>)——
//   当时靠直觉抄对了，但"展开口述"没验证过（导师记了账）。今天回炉：先把两种写法
//   逐属性纸面展开、和编辑器对照，再把 update 换成白名单版，顺手用 Record 给标签计数
//   ——体会"类型也可以复用"：同一份 Contact，第三、四次长出新用法。
//
// 规则：全程禁 as / ！；命名 camelCase；顺序不能倒——先纸面展开（TODO 1–2），
//   再动 ex4 的代码（TODO 4 起）；改完 ex4 记得回 npx tsc --noEmit 验沉默

// ======================= 纸面展开（先手写，再验证）=======================
// TODO 1) 第 2 课的黑名单写法，逐属性展开（纸面写，先不跑代码）：
//        Partial<Omit<Contact, 'id' | 'tags'>> 展开 = { ______________________ }
// TODO 2) 白名单写法，逐属性展开：
//        Partial<Pick<Contact, 'name' | 'phone'>> 展开 = { ______________________ }
// TODO 3) 一句话结论：两个展开相等吗？Pick 和 Omit 是什么关系——
//        （提示：Contact 一共几个字段？"去掉 id 和 tags"与"挑出 name 和 phone"……）

// ======================= 回炉 ex4（代码改动落那边）=======================
// TODO 4) 打开 01_语法起步/ex4_contacts.ts，把 update 的 changes 参数类型换成
//        Partial<Pick<Contact, 'name' | 'phone'>>（只换类型，行为一字不动）；
//        换完把鼠标悬停在参数类型上，和 TODO 1/2 的纸面展开对照——
//        第 2 课欠的"展开口述"，今天补验，对照结果写进下面留痕区
// TODO 5) 还在 ex4：挨着 findByTag 新增 countByTag()——遍历 contacts，
//        统计每个合法标签出现在几个联系人身上，返回 Record<Tag, number>；
//        注意 tags 是数组：Bob 带 ["家人", "同事"]，两边都要数到（不是只数第一个）；
//        演示区加一行 console.log(countByTag())，输出和通讯录现状对得上
// TODO 6) 漏键实验：初始化计数表时故意漏掉一个标签（比如不写 "工作"）→
//        跑 npx tsc --noEmit，抄报错原文到下面留痕区 → 修回，恢复沉默

// ======================= 留痕区（判卷证据，逐条写）=======================
// TODO 1 展开（黑名单版）：
// TODO 2 展开（白名单版）+ 与悬停对照的结果：
// TODO 3 一句话结论：
// TODO 6 漏键报错原文：

// 完成判据：ex4 的 update 已换白名单版且 npx tsc --noEmit 沉默 + countByTag 计数正确
//   （双标签的人两头都数到）+ 漏键报错留痕后已修回 + 本文件留痕四条齐全
