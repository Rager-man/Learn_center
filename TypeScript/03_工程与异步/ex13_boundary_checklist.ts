// 03_工程与异步/ex13_boundary_checklist.ts —— 骨架（第 6 课 · 任务 2：边界清单）
// 用法：npx tsx 03_工程与异步/ex13_boundary_checklist.ts   （骨架从出生就是绿的；
//   这个文件是审计台——清单和留痕写在这，代码改动落在 ex12）
//
// 剧本：ex12 能跑 ≠ 处处设防。数一数天气 CLI 一共有几个"数据进口"，
//   逐个检查有没有校验、是哪一关；没校验的补上（守卫函数写进 ex12），然后补前/补后对比。
//
// 规则：全程禁 as / ！；命名 camelCase；清单写全比写得快重要。

// ======================= 数据进口清单（TODO 1、2 在这写） =======================
// 判断标准一句话：这份数据进我的进程之前，谁看过它？——没人看过、验过的，都是进口。
//   （注意：URL 字符串不是进口——它是你自己拼的，编译器全程盯着；想想为什么）
//
// 进口 #1：process.argv（命令行参数）
//   有校验吗：补前没有——坏输入直达 fetch，被 open-meteo 用 400 打回；
//     补后在 parseCoords 三关拦下：关①数量（缺参 39.9）、关②NaN（abc）、关③范围（999）
// 进口 #2：API 响应。有校验吗：有——fetch 路径 1/2/3 + zod 第四关，四关全在它身上
// 进口 #x：process.env（环境变量）——今天用到了吗？：TODO 标注 没用到
// 进口 #x：文件内容——今天用到了吗？：TODO 标注（第 9 课的待办 CLI 全用上） 没用到

// ======================= 补防（代码写进 ex12，演示留痕写在这） =======================
// TODO 3) 在 ex12 里写 parseCoords(args: string[])，三关：
//   ① 个数不对（args.length !== 2 就拦）
//   ② NaN（Number("abc") 的下场——第 1 课就见过；判法用 Number.isNaN）
//   ③ 越界（纬度 -90~90、经度 -180~180——规则抄 open-meteo 那句 400 报错的）
//   写完把 ex12 的裸取换成 parseCoords。

// ======================= 补前 vs 补后（判卷证据） =======================
// TODO 4) 三个坏输入，补防前后各跑一遍，留痕对比：
//   "npx tsx ... abc 116.4" —— 补前：走到fetch；补后：第②关拦下，打出:参数非法，传入参数要求均为数字
//   "npx tsx ... 999 116.4" —— 补前：路径 2 拦下，打出fetch 成功但返回不 ok，status 是：400；补后：打出维度需在-90~90, 经度需在-180~180
//   "npx tsx ... 39.9"      —— 补前：fetch 成功但返回不 ok，status 是：400；补后：参数数量非法，只允许输入两个参数
//
// 一句结论（把关口修在哪最划算、错误离根因多远）： 把关口修在外部参数进口处拦下，从源头终止错误

// 完成判据：清单 ≥2 个在用进口且各有校验标注（未用的两个也各占一行）；
//   parseCoords 三关在 ex12 生效；三个坏输入补前/补后对比留痕齐全 + 一句结论；
//   正常输入 39.9 116.4 仍能出结果；npx tsc --noEmit 沉默
