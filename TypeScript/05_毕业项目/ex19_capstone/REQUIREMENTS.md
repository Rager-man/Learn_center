# 毕业项目需求——5 行开工单（任务 0）

> **规矩：不写完这 5 行，一行代码都不许动。**（第 3 课起你就知道了：类型建模的第一步是把数据的形状想清楚——项目建模的第一步，是把"交付什么"想清楚。）
> 写完发我过目一眼再开工——不是审批，是确保你没把 2 小时的活定成 2 天的。

## 二选一（勾一个）

- [ ] **选项 A · GitHub 仓库体检 CLI**：输入用户名 → 拉取仓库列表 → zod 校验 → 一页体检报告（总数 / 最近更新 / 语言分布）
- [ ] **选项 B · AI 调用加固器**：把 AI_agent 第 1、2 课的练习用 TS 课的纪律重做——LLM 调用 + zod 结构化输出 + 解析失败重试兜底

（骨架里 A、B 两份照抄块都给了——选定后把没用的那半**整段删掉**，别留死注释，判卷先扫。）

## 5 行需求（照着范例填，每行一句话就够）

| # | 要回答什么 | 范例（选项 A 版） | 我的版本 |
|---|---|---|---|
| 1 | 一句话：这个工具做什么 | 输入 GitHub 用户名，输出该用户公开仓库的一页体检报告 | |
| 2 | 输入是什么、从哪进来 | argv[2] 一个用户名（string）；无其它进口 | |
| 3 | 输出长什么样（手画 ≥3 行） | `仓库总数：8` / `最近推送：2024-05-26 boysenberry-repo-1` / `语言分布：TypeScript 3、未标注 5` | |
| 4 | 错误路径清单（≥4 条，每条配一句人话） | 空输入 / 用户名不存在(404) / 断网 / 限额(403) | |
| 5 | 复用清单（从哪个 ex 搬什么） | ex12 三条错误路径 → api.ts；ex18 parseDb 两层坏 → core.ts | |

## 硬性要求（缺一不可，验收对账用）

1. strict 下 0 error，全程无 `any`（`unknown` + 守卫/zod 是唯一逃生门）
2. 每条错误路径（空输入、断网、非 2xx、坏数据）都有人话，程序不裸崩
3. 核心逻辑 ≥5 个测试（只测纯函数，不碰网络）
4. README：别人照着能跑起来 + "已知局限"一节
